// Generated-style bindings, excluded from analysis by analysis_options.yaml.
// scip-dart used to index only the analyzer's analyzedFiles(), so this file and
// its references vanished (fail-open). Phase 2 fix round 3 (fork patch 10).
import '../bindings_backend.dart';

// Expected: alive, no finding (external ref from acme_app bin/clock.dart).
int bindingsCall() => bindingsBackend();
