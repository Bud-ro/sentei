// Flutter runner stub: platform code talks to Dart over method channels and cannot
// import acme_widgets, so discover must not flag the app unindexed_consumer for it.
package com.acme.app

import io.flutter.embedding.android.FlutterActivity

class MainActivity : FlutterActivity()
