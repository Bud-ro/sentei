name: acme_widgets
description: "Org-private Flutter widget library (depends on the Flutter SDK)."
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
