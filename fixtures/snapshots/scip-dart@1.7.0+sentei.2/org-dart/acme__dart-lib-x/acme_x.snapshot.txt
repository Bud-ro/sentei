# sentei scip snapshot: scip-dart 1.7.0
# positions are 0-based line:character (UTF-16); symbol versions normalised to `.`

## lib/acme_x.dart
  // Package entry point for acme_x (lib/*.dart, not lib/src/): defines the export surface.
//^ definition scip-dart pub acme_x . lib/`acme_x.dart`/
  library;

  // Re-export of a whole src/ library: implUsed and implUnused join the surface.
  export 'src/impl.dart';
  // `show` combinator: only Shown joins the surface; Hidden does not.
  export 'src/shown.dart' show Shown;
//                             ^^^^^ reference scip-dart pub acme_x . lib/src/`shown.dart`/Shown#

  // A part file: its declarations belong to this library, so they are surface too.
  part 'src/part_a.dart';

  // Expected: alive, no finding (external ref from acme_app bin/main.dart, unqualified call).
  int usedFn() => _privateFn() + 1;
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^ definition scip-dart pub acme_x . lib/`acme_x.dart`/usedFn(). enclosing [12:0-12:33]
//                ^^^^^^^^^^ reference scip-dart pub acme_x . lib/`acme_x.dart`/_privateFn().

  // Expected: deletion_candidate, reasons ["no_refs"] (public in the entry file, referenced nowhere).
  int unusedFn() => 0;
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^^^ definition scip-dart pub acme_x . lib/`acme_x.dart`/unusedFn(). enclosing [15:0-15:20]

  // Expected: alive, no finding (private, never exported; reachable via usedFn).
  int _privateFn() => 1;
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^^^^^ definition scip-dart pub acme_x . lib/`acme_x.dart`/_privateFn(). enclosing [18:0-18:22]

  // Expected: private_dead, reasons ["already_unreachable"] (island with _islandB, reached by nothing).
  // ignore: unused_element
  int _islandA(int n) => n <= 0 ? 0 : _islandB(n - 1);
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^^^ definition scip-dart pub acme_x . lib/`acme_x.dart`/_islandA(). enclosing [22:0-22:52]
//             ^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//                 ^ definition local 0 enclosing [22:13-22:18]
//                       ^ reference local 0
//                                    ^^^^^^^^ reference scip-dart pub acme_x . lib/`acme_x.dart`/_islandB().
//                                             ^ reference local 0

  // Expected: private_dead, reasons ["already_unreachable"] (island with _islandA, reached by nothing).
  int _islandB(int n) => n <= 0 ? 1 : _islandA(n - 1);
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^^^ definition scip-dart pub acme_x . lib/`acme_x.dart`/_islandB(). enclosing [25:0-25:52]
//             ^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//                 ^ definition local 1 enclosing [25:13-25:18]
//                       ^ reference local 1
//                                    ^^^^^^^^ reference scip-dart pub acme_x . lib/`acme_x.dart`/_islandA().
//                                             ^ reference local 1

  // Expected: alive, no finding. Used only implicitly by acme_app (`3.doubled`):
  // no identifier in the consumer names IntTimes, only its member.
  extension IntTimes on int {
//          ^^^^^^^^ definition scip-dart pub acme_x . lib/`acme_x.dart`/IntTimes# enclosing [29:0-32:1]
//                      ^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
    // Expected: alive, no finding (member of a live extension; external ref `3.doubled`).
    int get doubled => this * 2;
//  ^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//          ^^^^^^^ definition scip-dart pub acme_x . lib/`acme_x.dart`/IntTimes#`<get>doubled`. enclosing [31:2-31:30]
  }

## lib/src/impl.dart
  // Re-exported whole by lib/acme_x.dart (`export 'src/impl.dart';`).
//^ definition scip-dart pub acme_x . lib/src/`impl.dart`/

  // Expected: alive, no finding (external ref from acme_app bin/main.dart).
  int implUsed() => 2;
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^^^ definition scip-dart pub acme_x . lib/src/`impl.dart`/implUsed(). enclosing [3:0-3:20]

  // Expected: deletion_candidate, reasons ["no_refs"] (surface via export, referenced nowhere).
  int implUnused() => 3;
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^^^^^ definition scip-dart pub acme_x . lib/src/`impl.dart`/implUnused(). enclosing [6:0-6:22]

  // Expected: alive, no finding. acme_app names it in an import `show` list but never
  // calls it; a shown name counts as a reference (fail closed, like a TS import binding).
  int shownOnly() => 6;
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^^^^ definition scip-dart pub acme_x . lib/src/`impl.dart`/shownOnly(). enclosing [10:0-10:21]

## lib/src/part_a.dart
  // Part of the entry library: lives under lib/src/ but its public names are on the
//^ definition scip-dart pub acme_x . lib/src/`part_a.dart`/
  // acme_x surface because the library is lib/acme_x.dart.
  part of '../acme_x.dart';

  // Expected: alive, no finding (external ref from acme_app bin/main.dart).
  int partUsed() => 4;
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^^^ definition scip-dart pub acme_x . lib/src/`part_a.dart`/partUsed(). enclosing [5:0-5:20]

  // Expected: deletion_candidate, reasons ["no_refs"] (surface via the part, referenced nowhere).
  int partUnused() => 5;
//^^^ reference scip-dart pub dart:core . dart:core/`int.dart`/int#
//    ^^^^^^^^^^ definition scip-dart pub acme_x . lib/src/`part_a.dart`/partUnused(). enclosing [8:0-8:22]

## lib/src/shown.dart
  // Re-exported by lib/acme_x.dart with `show Shown`: Hidden is NOT on the surface.
//^ definition scip-dart pub acme_x . lib/src/`shown.dart`/

  // Expected: alive, no finding (external ref: acme_app constructs Shown() and reads label).
  class Shown {
//      ^^^^^ definition scip-dart pub acme_x . lib/src/`shown.dart`/Shown# enclosing [3:0-6:1]
    // Expected: alive, no finding (member of a live class).
    final String label = 'shown';
//        ^^^^^^ reference scip-dart pub dart:core . dart:core/`string.dart`/String#
//               ^^^^^ definition scip-dart pub acme_x . lib/src/`shown.dart`/Shown#label. enclosing [5:15-5:30]
  }

  // Expected: private_dead, reasons ["already_unreachable"]. Public name in lib/src/, but
  // the only export of this file is `show Shown`, so Hidden is not on the surface and
  // nothing reaches it. It must never be a deletion_candidate (it is not an export).
  class Hidden {
//      ^^^^^^ definition scip-dart pub acme_x . lib/src/`shown.dart`/Hidden# enclosing [11:0-14:1]
    // Expected: no finding (member of Hidden; only the outermost dead declaration is reported).
    final String label = 'hidden';
//        ^^^^^^ reference scip-dart pub dart:core . dart:core/`string.dart`/String#
//               ^^^^^ definition scip-dart pub acme_x . lib/src/`shown.dart`/Hidden#label. enclosing [13:15-13:31]
  }
